fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pypdf>=3.0.0

# Optional: Docling for structure-aware PDF parsing (falls back to pypdf if not installed)
# docling>=2.70.0
# sentence-transformers>=2.2.0
